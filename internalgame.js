function handleinput({
    gameStarted,
    leftPressed,
    rightPressed,
    forwardPressed,
    backPressed,
    spacePressed,
    keyPressed,
    player,
}) {
    if (gameStarted) {
	if (leftPressed) {
	}
	if (rightPressed) {
	}
	if (forwardPressed) {
	}
	if (backPressed) {
	}
	if (spacePressed) {
	}
	
    } else {
	if (keyPressed) {
	    state.gameStarted = true;
	}
    }
}

function advance(state) {
    
}

function projectiles(state) {
    
}

function hit_detection(state) {
    
}

function startMonster(state) {
    
}
